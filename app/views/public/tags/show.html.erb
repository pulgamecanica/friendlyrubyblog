<% content_for :title, "#{@tag.title} · Tag" %>

<h1 class="text-2xl font-semibold mb-6">#<%= @tag.title %></h1>

<div class="grid sm:grid-cols-2 gap-6">
  <% @documents.each do |doc| %>
    <%= render partial: "public/documents/card", locals: { doc: doc } %>
  <% end %>
</div>

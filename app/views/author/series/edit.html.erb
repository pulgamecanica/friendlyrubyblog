<% content_for :title, "Edit Series · #{@series.title}" %>

<div class="flex items-center justify-between mb-6">
  <h1 class="text-2xl font-semibold">Edit Series</h1>
  <div class="space-x-3">
    <%= link_to "View Public", public_series_path(@series), target: "_blank",
          class: "text-sm text-gray-600 hover:underline" %>
    <%= link_to "Back", author_series_index_path, class: "text-sm text-gray-600 hover:underline" %>
  </div>
</div>

<%= render "form", series: @series %>

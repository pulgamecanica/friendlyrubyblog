<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Author · Friendlyrubyblog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gray-100">
    <header class="bg-white border-b">
      <div class="mx-auto max-w-6xl flex items-center justify-between px-6 py-4">
        <%= link_to "Author Dashboard", author_root_path, class: "font-semibold tracking-tight" %>

        <nav class="space-x-4 text-sm">
          <%= link_to "Documents", author_documents_path, class: "hover:underline" %>
          <%= link_to "Series", author_series_index_path, class: "hover:underline" %>
          <%= link_to "Comments", author_comments_path, class: "hover:underline" %>
          <%= link_to "Logout", destroy_author_session_path, method: :delete, class: "text-red-600 hover:underline" %>
        </nav>
      </div>
    </header>

    <!-- Flash -->
    <div class="mx-auto max-w-3xl px-4">
      <% if notice.present? %>
        <p class="mt-4 rounded-md bg-green-50 text-green-700 px-4 py-2 text-sm"><%= notice %></p>
      <% end %>
      <% if alert.present? %>
        <p class="mt-4 rounded-md bg-red-50 text-red-700 px-4 py-2 text-sm"><%= alert %></p>
      <% end %>
    </div>

    <main class="">
      <%= yield %>
    </main>
  </body>
</html>

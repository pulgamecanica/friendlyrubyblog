<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Author · Friendlyrubyblog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gray-100 dark:bg-gray-900 transition-colors" data-controller="theme" data-theme-default-value="dark">
    <!-- Toast container for turbo stream appends -->
    <div id="toast_container" class="toast-container"></div>

    <header class="bg-white dark:bg-gray-800 border-b dark:border-gray-700 transition-colors">
      <div class="mx-auto max-w-7xl px-6 flex items-center justify-between py-4">
        <%= link_to "Author Dashboard", author_root_path, class: "font-semibold tracking-tight dark:text-white" %>

        <nav class="flex items-center space-x-4 text-sm">
          <%= link_to "Documents", author_documents_path, class: "hover:underline dark:text-gray-300" %>
          <%= link_to "Series", author_series_index_path, class: "hover:underline dark:text-gray-300" %>
          <%= link_to "Comments", author_comments_path, class: "hover:underline dark:text-gray-300" %>

          <!-- Theme Toggle -->
          <button data-action="click->theme#toggle"
                  class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors"
                  title="Toggle theme">
            <svg class="w-5 h-5 dark:text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>
            </svg>
          </button>

          <%= link_to "Logout", destroy_author_session_path, method: :delete, class: "text-red-600 dark:text-red-400 hover:underline" %>
        </nav>
      </div>
    </header>

    <!-- Fallback for non-JS users -->
    <noscript>
      <div class="mx-auto max-w-3xl px-4">
        <% if notice.present? %>
          <p class="mt-4 rounded-md bg-green-50 text-green-700 px-4 py-2 text-sm"><%= notice %></p>
        <% end %>
        <% if alert.present? %>
          <p class="mt-4 rounded-md bg-red-50 text-red-700 px-4 py-2 text-sm"><%= alert %></p>
        <% end %>
      </div>
    </noscript>

    <main class="">
      <%= yield %>
    </main>
  </body>
</html>

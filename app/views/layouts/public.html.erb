<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Friendlyrubyblog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body id="public" class="min-h-screen bg-white dark:bg-gray-950 text-gray-900 dark:text-gray-300 antialiased transition-colors" data-controller="theme" data-theme-default-value="dark" style="background-image: url('data:image/svg+xml,%3Csvg width=\"30\" height=\"30\" xmlns=\"http://www.w3.org/2000/svg\"%3E%3Cpath d=\"M0 0h30v30H0z\" fill=\"none\"/%3E%3Ccircle cx=\"1\" cy=\"1\" r=\"0.5\" fill=\"%23374151\" opacity=\"0.15\"/%3E%3C/svg%3E');">
    <!-- Top nav -->
    <header class="sticky top-0 z-50 border-b border-gray-200 dark:border-gray-800 bg-white/95 dark:bg-gray-950/95 backdrop-blur-sm transition-colors">
      <div class="mx-auto max-w-6xl px-4 sm:px-6">
        <div class="flex h-14 items-center justify-between">
          <!-- Logo -->
          <%= link_to root_path, class: "group flex items-center gap-3 hover:text-green-500 transition-colors font-mono" do %>
            <span class="text-green-600 dark:text-green-500 text-lg">~</span>
            <span class="text-base text-gray-700 dark:text-gray-400 group-hover:text-green-600 dark:group-hover:text-green-500 transition-colors">/home/pulgamecanica</span>
          <% end %>

          <!-- Navigation -->
          <nav class="flex items-center gap-6 text-sm font-mono">
            <%= link_to public_posts_path, class: "text-gray-600 dark:text-gray-500 hover:text-green-600 dark:hover:text-green-500 transition-colors" do %>
              posts
            <% end %>

            <% if author_signed_in? %>
              <%= link_to public_notes_path, class: "text-gray-600 dark:text-gray-500 hover:text-green-600 dark:hover:text-green-500 transition-colors" do %>
                notes
              <% end %>
            <% end %>

            <%= link_to public_series_index_path, class: "text-gray-600 dark:text-gray-500 hover:text-green-600 dark:hover:text-green-500 transition-colors" do %>
              series
            <% end %>

            <!-- Pages Dropdown -->
            <div class="relative" data-controller="collapsible" data-collapsible-expanded-value="false">
              <button type="button"
                      data-action="click->collapsible#toggle"
                      data-collapsible-target="trigger"
                      class="inline-flex items-center gap-1 text-sm font-mono text-gray-600 dark:text-gray-500 hover:text-green-600 dark:hover:text-green-500 transition-colors">
                <span>pages</span>
                <svg data-collapsible-target="icon"
                     class="h-3 w-3 transition-transform duration-200"
                     fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
              </button>

              <!-- Dropdown Menu -->
              <div data-collapsible-target="content"
                   class="hidden absolute right-0 mt-2 w-80 bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 py-2 z-50 max-h-96 overflow-y-auto shadow-xl transition-colors">
                <div class="px-4 py-2 border-b border-gray-200 dark:border-gray-800">
                  <%= link_to public_pages_path, class: "text-xs font-mono text-green-600 dark:text-green-500 hover:text-green-700 dark:hover:text-green-400" do %>
                    → all pages
                  <% end %>
                </div>

                <% Series.order(:title).each do |series| %>
                  <% pages = series.documents.published.where(kind: "page").order(:series_position) %>
                  <% next if pages.empty? %>

                  <div class="px-4 py-3 border-b border-gray-100 dark:border-gray-800 last:border-0">
                    <!-- Series Name -->
                    <div class="text-xs font-mono text-gray-500 dark:text-gray-600 mb-2">
                      # <%= series.title %>
                    </div>

                    <!-- Pages -->
                    <div class="space-y-1">
                      <% pages.limit(5).each do |page| %>
                        <%= link_to public_page_path(page), class: "block px-2 py-1 text-sm text-gray-700 dark:text-gray-400 hover:text-green-600 dark:hover:text-green-500 transition-colors font-mono" do %>
                          <span class="text-green-600 dark:text-green-500 mr-1">›</span><%= page.title %>
                        <% end %>
                      <% end %>
                      <% if pages.count > 5 %>
                        <div class="px-2 py-1 text-xs text-gray-500 dark:text-gray-600 font-mono">
                          +<%= pages.count - 5 %> more...
                        </div>
                      <% end %>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>

            <span class="text-gray-400 dark:text-gray-700">|</span>

            <!-- Theme Toggle -->
            <button data-action="click->theme#toggle"
                    class="text-xs font-mono text-gray-500 dark:text-gray-600 hover:text-green-600 dark:hover:text-green-500 transition-colors"
                    title="Toggle theme">
              <span class="hidden dark:inline">[light]</span>
              <span class="inline dark:hidden">[dark]</span>
            </button>
          </nav>
        </div>
      </div>
    </header>

    <!-- Flash -->
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <% if notice.present? %>
        <div class="mt-4 rounded-xl bg-gradient-to-r from-green-50 to-emerald-50 border border-green-200 text-green-800 px-6 py-4 text-sm shadow-sm">
          <div class="flex items-center gap-2">
            <svg class="h-5 w-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <%= notice %>
          </div>
        </div>
      <% end %>
      <% if alert.present? %>
        <div class="mt-4 rounded-xl bg-gradient-to-r from-red-50 to-rose-50 border border-red-200 text-red-800 px-6 py-4 text-sm shadow-sm">
          <div class="flex items-center gap-2">
            <svg class="h-5 w-5 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <%= alert %>
          </div>
        </div>
      <% end %>
    </div>

    <!-- Main -->
    <main class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-12">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="mt-20 border-t border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-950 transition-colors">
      <div class="mx-auto max-w-6xl px-4 sm:px-6 py-8">
        <div class="flex flex-col items-center gap-3 text-sm font-mono">
          <div class="flex items-center gap-4 text-gray-600 dark:text-gray-500">
            <a href="https://github.com/pulgamecanica" target="_blank" rel="noopener" class="hover:text-green-600 dark:hover:text-green-500 transition-colors flex items-center gap-2">
              <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 24 24">
                <path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" clip-rule="evenodd" />
              </svg>
              github
            </a>
          </div>
          <p class="text-xs text-gray-500 dark:text-gray-600">
            made with <span class="text-red-500">♥</span> by pulgamecanica
          </p>
        </div>
      </div>
    </footer>
  </body>
</html>
